// This is here to be compatible with the current version of scratch-ui
// which accesses these fields directly from the vm
BlockType = require('scratch-vm-default/src/extension-support/block-type');
module.exports = BlockType;
